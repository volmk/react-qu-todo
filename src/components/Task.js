import React, {Fragment} from 'react';

const Task = () => {
    return (
        <Fragment>
            {/*<input type="checkbox" checked={done}/> {text}*/}
            hellod
        </Fragment>
    );
};

export default Task;